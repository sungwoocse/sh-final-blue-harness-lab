# This is a study-only outline, not a production-ready Harness export.
pipeline:
  name: builder-ci-outline
  identifier: builder_ci_outline
  description: Study outline for the first Harness pipeline in this lab
  stages:
    - name: Test
      type: ci
      steps:
        - run: install dependencies
        - run: execute tests
    - name: Build
      type: ci
      steps:
        - run: build container image
    - name: Push
      type: ci
      steps:
        - run: push image to registry
